name: Build & Release Firmware

# Dispara ao criar uma tag como "v1.0.0"
on:
  push:
    tags:
      - "v*"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # 1) Checkout do repositório
      - name: Checkout
        uses: actions/checkout@v4

      # 2) Instala o arduino-cli
      - name: Instalar arduino-cli
        uses: arduino/setup-arduino-cli@v2

      # 3) Adiciona o índice de placas e bibliotecas
      - name: Configurar índice de placas
        run: |
          arduino-cli config init
          arduino-cli config add board_manager.additional_urls \
            https://arduino.esp8266.com/stable/package_esp8266com_index.json
          arduino-cli core update-index
          arduino-cli core install esp8266:esp8266
          arduino-cli lib install "PubSubClient"
          arduino-cli lib install "ESP8266Ping"

      # 4) Extrai a versão da tag (remove o "v" inicial)
      - name: Extrair versão
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      # 5) Garante que version.h está sincronizado com a tag
      - name: Verificar versão em version.h
        run: |
          TAG_VERSION="${{ steps.version.outputs.VERSION }}"
          FILE_VERSION=$(grep -oP '^#define FIRMWARE_VERSION "\K[^"]+' mqtt-esp8266/version.h)
          if [ "$TAG_VERSION" != "$FILE_VERSION" ]; then
            echo "❌  Tag v$TAG_VERSION ≠ version.h ($FILE_VERSION)"
            echo "     Atualize FIRMWARE_VERSION em version.h antes de criar a tag."
            exit 1
          fi
          echo "✅  Versão $TAG_VERSION OK"

      # 6) Compila o sketch
      - name: Compilar firmware
        run: |
          arduino-cli compile \
            --fqbn esp8266:esp8266:nodemcuv2 \
            --output-dir build \
            mqtt-esp8266/mqtt-esp8266.ino

      # 7) Renomeia para firmware.bin e cria version.txt
      - name: Preparar assets
        run: |
          cp build/mqtt-esp8266.ino.bin firmware.bin
          echo "${{ steps.version.outputs.VERSION }}" > version.txt

      # 8) Verifica se o binário é um firmware ESP8266 válido (magic byte 0xE9)
      - name: Verificar magic byte do firmware
        run: |
          MAGIC=$(xxd -p -l 1 firmware.bin)
          echo "Magic byte: 0x$MAGIC"
          if [ "$MAGIC" != "e9" ]; then
            echo "❌ Binário inválido — esperado 0xe9, encontrado 0x$MAGIC"
            exit 1
          fi
          echo "✅ Firmware válido"

      # 9) Cria o release e faz upload dos assets
      - name: Criar Release no GitHub
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "Firmware ${{ github.ref_name }}"
          body: |
            ## Firmware ${{ github.ref_name }}
            
            Arquivos disponíveis:
            - `firmware.bin` — binário para gravação OTA
            - `version.txt`  — versão atual (`${{ steps.version.outputs.VERSION }}`)
          files: |
            firmware.bin
            version.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
